# Generated by aurora@aurora-smart-home (ha-integration-dev skill)
# https://github.com/tonylofgren/aurora-smart-home
name: Release package

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.14"
      - name: Validate package contents
        run: python scripts/check_package.py
      - name: Build package
        run: python -c "from pathlib import Path; from scripts.check_package import build_archive; build_archive(Path('dahua_arc_ha_v0.6.0.zip'))"
      - name: Upload package artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02d # v4.6.2
        with:
          name: dahua_arc_ha_v0.6.0
          path: dahua_arc_ha_v0.6.0.zip
          if-no-files-found: error

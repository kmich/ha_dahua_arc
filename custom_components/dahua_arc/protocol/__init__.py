"""Home Assistant-independent ARC3800H protocol layer.

Nothing in this package imports Home Assistant, so the protocol engine can be
unit-tested standalone and later extracted into a separate library.
"""

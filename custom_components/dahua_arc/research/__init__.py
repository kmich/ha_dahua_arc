"""Experimental PIRCam / LowRateWPAN research features.

Everything in this package is started only when the user explicitly enables
``enable_research_features``. It must never be required by the production
wired-zone path.
"""
